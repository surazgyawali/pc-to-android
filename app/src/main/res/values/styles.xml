<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="themeSwitch" parent="ThemeOverlay.AppCompat.Dark">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/teal</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/white</item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/white</item>
    </style>

    <style name="AlertDialog" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="dialogTheme">@color/colorBackground</item>
    </style>

</resources>
